fastapi==0.104.1
uvicorn[standard]==0.24.0
tensorflow-cpu==2.17.0
keras==3.10.0
tf-keras==2.17.0
optree==0.12.1
pillow==10.4.0            # 10.4 fixes CVEs present in 10.1 (Image.open path)
python-multipart==0.0.18  # 0.0.7+ fixes CVE-2024-24762 multipart ReDoS DoS
pydantic==2.5.0
slowapi==0.1.9
reportlab==4.5.1
python-dotenv==1.0.1
# Testing
pytest==8.1.1
# httpx capped <0.28: 0.28 removed the TestClient(app=...) shortcut that
# Starlette 0.27 (via FastAPI 0.104) relies on. Newer httpx breaks test collection.
httpx==0.27.0
